name: <NAME>
description: <DESCRIPTION>
publish_to: 'none'
version: 1.0.0+1

environment:
    <SDK>

dependencies:
  flutter:
    sdk: flutter
  # TODO MIGHT NEED TO ADJUST RELATIVE INPUT
  ltogt_utils_flutter:
    path: <HOME>/repos/package/ltogt_utils_flutter
  relation_canvas:
    path: <HOME>/repos/package/relation_canvas

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg

  # To add custom fonts to your application, add a fonts section here:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
